import React from 'react';

const Login = () => {
  return (
    <div>
      <h1>Login / Role Selection (Main Dashboard)</h1>
      <button>Admin</button>
      <button>Support</button>
    </div>
  );
};

export default Login;
